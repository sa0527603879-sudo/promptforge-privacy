# חיבור קלוד ל‑REAPER — ההתקנה בפקודה אחת

התיקייה הזאת מכילה את מה שהמדריך ביקש ממך לעשות ביד. הסקריפטים עושים את זה במקומך.

## מה שצריך פעם אחת, לפני הכול

1. **להתקין REAPER** — <https://www.reaper.fm/download.php>
   ואז **לפתוח אותו פעם אחת ולסגור**. זה מה שיוצר את תיקיית ההגדרות שהסקריפט מחפש.
2. **להתקין Claude Desktop** — <https://claude.ai/download>

## ההתקנה

### Windows

פותחים PowerShell בתיקייה הזאת ומדביקים:

```powershell
powershell -ExecutionPolicy Bypass -File .\setup-windows.ps1
```

### macOS

פותחים Terminal בתיקייה הזאת ומדביקים:

```bash
bash setup-macos.sh
```

הסקריפט מתקין `uv`, מעתיק את סקריפט הגשר לתיקיית REAPER, ורושם את השרת
בקובץ ההגדרות של Claude Desktop. אם כבר יש לך שרתים אחרים מוגדרים שם —
הם נשארים. הקובץ הישן נשמר כ‑`claude_desktop_config.json.backup` לפני כל שינוי.
אפשר להריץ שוב כמה פעמים שרוצים.

**REAPER נייד (portable)?** תוסיף את הנתיב:
`-ReaperScripts "D:\REAPER\Scripts"` בווינדוס, או
`REAPER_SCRIPTS=/path/to/REAPER/Scripts` לפני הפקודה במאק.

## שלושה דברים שהסקריפט לא יכול לעשות בשבילך

1. **לסגור את Claude Desktop לגמרי** — בווינדוס דרך מגש המערכת (לא ה‑X),
   במאק ‎Cmd+Q. ואז לפתוח מחדש. בלי זה הוא לא קורא את ההגדרות החדשות.
2. **לטעון את הגשר בתוך REAPER** — ‏`Actions → Show action list → Load ReaScript`,
   בוחרים `reaper_mcp_bridge.lua`, ‏`Run`.
   בקונסולה של REAPER אמור להופיע: `REAPER MCP Bridge (File-based, Full API) started`
3. **לבדוק** — בשיחה חדשה בקלוד: «כמה טראקים יש לי בפרויקט ב‑REAPER?»
   חזר מספר — הכול מחובר.

> חשוב: שלב 2 צריך לחזור **אחרי כל עדכון של החבילה**. הגשר והשרת חייבים להיות
> באותה גרסה, אחרת הכלים נכשלים בשקט.

## חבילת ה‑MIDI להתחלה

`midi/` — ארבעה לופים של 8 תיבות, **130 BPM, לה מינור**.

המספרים האלה לא שרירותיים: הם נלקחו מהניתוח של **Bicep — "Opal"**
(‏130 BPM, לה מינור, Camelot 8A, danceability 0.80), שזה בדיוק הסאונד
המלודי‑האוס המקצועי שאתה מכוון אליו.

| קובץ | מה זה |
|---|---|
| `chords_Am_130.mid` | Am9 → Fmaj9 → Cmaj7 → G |
| `bass_Am_130.mid` | בס על האוף‑ביט, משאיר מקום לקיק |
| `arp_Am_130.mid` | ארפג׳יו בשמיניות‑עשרה על שתי אוקטבות |
| `drums_130.mid` | קיק על כל רבע, מחיאה על 2 ו‑4, הייהאט על האוף |

גוררים ישר לתוך REAPER. הקצב נמצא בתוך הקבצים, אז הפרויקט יתיישר לבד.

לשנות סולם או טמפו: עורכים את `BPM` / `PROGRESSION` בראש `make_midi.py`
ומריצים `python3 make_midi.py`.

## מה בודק את עצמו

- שני הסקריפטים הורצו כאן מקצה לקצה — התקנה נקייה, מיזוג לקובץ הגדרות קיים,
  קובץ הגדרות פגום (נכשל בלי להרוס), ו‑REAPER לא מותקן.
- קבצי ה‑MIDI נבדקו בפרסר חיצוני (`mido`): טמפו נכון, כל `note_on` עם
  `note_off` תואם, התופים על ערוץ 10.
